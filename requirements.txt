# Nivra — lokal mailprovider
#
# Systempaket (Debian/Ubuntu):
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
#
# Inga pip-beroenden krävs för v1 (stdlib + PyGObject från systemet).

# Optional future deps can go here, e.g.:
# aiosmtpd>=1.4.0
